# 复制为 .env 后按需填写（.env 已在 .gitignore 中，不会提交）

# X 官方 API Bearer Token —— 到 developer.x.com 申请（注意：tweets/search/recent 需要付费计划，免费层无此接口）
X_API_BEARER=your_bearer_token_here

# GitHub PAT（可选）：搜索 API 限额 60 次/h → 5000 次/h。线上建议配成 GitHub Actions 的 GH_TOKEN secret
GH_TOKEN=
